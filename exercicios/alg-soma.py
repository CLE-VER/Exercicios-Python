n1 = int(input('Insira o primeiro número: ')) # Cria uma variável que vai guardar o valor inserido pelo usuário

n2 = int(input('insira o segundo número: ')) # a mesma coisa da prmeira linha, mas para guardar o segundo valor digitado

soma = n1+n2 # cria uma nova variável para armazenar o resuldado da soma entre esses dois valores

print('A soma entre {} e {} é igual a {}'.format(n1,n2,soma)) #mostra o resultado